Running the Test locally

To compile the Test class selenium-server-standalone library is required . Add this library as
external jar in the project. Other required libraries are added as Maven dependencies.
